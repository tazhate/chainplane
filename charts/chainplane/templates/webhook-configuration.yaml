{{- if .Values.webhook.enabled }}
apiVersion: admissionregistration.k8s.io/v1
kind: ValidatingWebhookConfiguration
metadata:
  name: {{ include "chainplane.fullname" . }}-validating
  labels:
    {{- include "chainplane.labels" . | nindent 4 }}
webhooks:
  - admissionReviewVersions:
      - v1
    clientConfig:
      service:
        name: {{ include "chainplane.webhookServiceName" . }}
        namespace: {{ .Release.Namespace }}
        path: /validate-nodes-chainplane-io-v1alpha1-blockchainnode
    failurePolicy: {{ .Values.webhook.failurePolicy }}
    name: vblockchainnode.kb.io
    rules:
      - apiGroups:
          - nodes.chainplane.io
        apiVersions:
          - v1alpha1
        operations:
          - CREATE
          - UPDATE
        resources:
          - blockchainnodes
    sideEffects: None
{{- end }}
