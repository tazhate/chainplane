apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: {{ include "blockchain-node-operator.fullname" . }}-manager
  labels:
    {{- include "blockchain-node-operator.labels" . | nindent 4 }}
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: {{ include "blockchain-node-operator.fullname" . }}-manager
subjects:
  - kind: ServiceAccount
    name: {{ include "blockchain-node-operator.serviceAccountName" . }}
    namespace: {{ .Release.Namespace }}
