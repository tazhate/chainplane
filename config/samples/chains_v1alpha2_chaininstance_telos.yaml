apiVersion: chains.chainplane.io/v1alpha2
kind: ChainInstance
metadata:
  labels:
    app.kubernetes.io/name: chainplane
    app.kubernetes.io/managed-by: kustomize
  name: telos-mainnet
spec:
  chain: telos
  network: mainnet
  nodeType: rpc
  nodeGroup: heavy
  # Default image: telosnetwork/telos-evm-rpc:v2.0.0
  storage:
    size: 300Gi
    storageClass: fast-ssd
  resources:
    requests:
      cpu: "4"
      memory: 8Gi
    limits:
      cpu: "8"
      memory: 16Gi
  rpc:
    enabled: true
    port: 8545
    wsPort: 8546
  health:
    blockLagThreshold: 30
